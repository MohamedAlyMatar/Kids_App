import 'package:flutter/material.dart';

class PicsList extends StatelessWidget {
  const PicsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
